# Base URL for the Tilora backend API.
# Leave empty or unset during local dev to use Vite's proxy (/api -> backend),
# which allows zero-config testing across all devices on your local network.
# Set explicitly if frontend and backend run on separate custom hosts/ports.
PUBLIC_API_BASE_URL=

